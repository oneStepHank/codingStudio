name: Move LeetCode Solutions

on:
  workflow_dispatch:
  push:
    paths:
      - '[0-9][0-9][0-9][0-9]-*/**'

# 🔐 이 부분을 추가해 주세요!
permissions:
  contents: write

jobs:
  move-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Move LeetCode solutions
        run: |
          TARGET_FOLDER="LeetCode"
          mkdir -p $TARGET_FOLDER
          
          found=0
          for d in [0-9][0-9][0-9][0-9]-*; do
            if [ -d "$d" ]; then
              echo "Moving directory: $d to $TARGET_FOLDER/"
              mv "$d" "$TARGET_FOLDER/" && found=1
            fi
          done
          
          if [ $found -eq 0 ]; then
            echo "No matching directories found to move."
          fi

      - name: Push changes
        run: |
            git config --global user.name 'github-actions[bot]'
            git config --global user.email 'github-actions[bot]@users.noreply.github.com'
            git add .
            git commit -m "Organize LeetCode solutions"
            git push https://x-access-token:${{ secrets.LEETCODE_PUSH_TOKEN }}@github.com/oneStepHank/codingStudio.git HEAD:main